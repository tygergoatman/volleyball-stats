/**
 * Build identifier, shown on the Roster tab under Data.
 *
 * Its only job is to answer "is this phone running the version I just
 * published?" without guesswork. Bump it whenever you publish; nothing depends
 * on the format beyond being visibly different from the last one.
 */
export const APP_VERSION = '2026.08.19b';
